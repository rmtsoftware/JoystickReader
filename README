Создать виртуальное окружение: `py -3 -m venv venv`  

Активировать venv: `.\venv\Scripts\activate`  

Установить необходимые библиотеки: `pip install -r .\requirements.txt`

Доустановить библиотеку LabJackPython: `python setup.py install`
